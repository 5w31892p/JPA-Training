spring:
  datasource: # ??? ??? ???? HibernateJpaAutoConfiguration ? ?? ????
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/messenger
    username: seowoo
    password: pass
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
      # show_sql: true
logging:
  level:
    org.hibernate.sql: debug
    org.hibernate.type: trace # ?? ?? ??

# ??: ?? ?? ??? ??? ??? ?? ??? ??.
#> show_sql : ??? System.out ? ?????? ?? SQL? ???.
#> org.hibernate.SQL : ??? logger? ?? ?????? ?? SQL? ???
